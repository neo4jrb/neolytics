require 'spec_helper'

describe Neolytics do
  it 'has a version number' do
    expect(Neolytics::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
